export interface CommitOptions {
  version: string;
  targetBranch: string;
}

export interface VersionOptions {
  version: string;
}

export interface TargetBranchOptions {
  targetBranch: string;
}
